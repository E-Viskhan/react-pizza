import React from 'react';
import { NotFoundBlock } from '../components';

const NotFound: React.FC = () => {
  console.log('this code is bad code');

  return <NotFoundBlock />;
};

export default NotFound;
